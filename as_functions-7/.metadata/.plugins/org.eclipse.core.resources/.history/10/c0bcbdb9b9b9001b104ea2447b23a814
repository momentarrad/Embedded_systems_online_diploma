/*
 * EX4.c
 *
 *  Created on: May 21, 2021
 *      Author: Ahmed
 */

int power_of_num(int num, int power)
{
	if(power==1)
		return num;
	else
	if(power==0)
		return 1;

	else
		return num*power(num,power-1);

}
